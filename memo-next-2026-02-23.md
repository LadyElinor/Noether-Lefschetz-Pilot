# Memo Next — NL Quartic Pilot (2026-02-23)

## Completed in this pass
1. Ran one larger deterministic random batch at target scale (1000 quartics).
2. Added stratified stats by coefficient bound (1,2,3) with smooth-pass and line-detection rates.
3. Implemented a conservative `--mode perturbation` for a **proxy** van-Luijk-style family.
4. Added reproducible CLI options with minimal script changes (single-file updates).
5. Produced:
   - `results-large-batch.md`
   - `results-stratified.md`
   - `results-perturbation.md`
   - `memo-next-2026-02-23.md`

## Theorem vs heuristic separation

### A) External theorem/literature context (not proved by this script)
- General Noether–Lefschetz/Picard-rank framework is external [NL-classical].
- Fermat quartic as a classically special surface is external [Fermat-classical].
- van Luijk’s arithmetic-rank style methods are external and not replicated here [vanLuijk-method].

### B) Exact algebraic checks inside this pilot
- Exact integer substitution check for candidate-line containment.
- Exact check of one fixed conic parametrization for the forced-conic construction.

### C) Heuristic-only outputs in this pilot
- Smoothness screen from sampled projective points modulo small primes.
- Line-incidence rates over a finite candidate-line bank.
- Perturbation-family detection rates for the proxy baseline.

## Conservative claim boundary
- No claim of Picard rank is made.
- No claim of NL-locus membership is made.
- Non-detection of lines in finite bank does not imply genericity.
- Perturbation mode is explicitly **not** van Luijk’s exact quartic unless exact polynomial data is added later.

## Reproducibility and runtime
- All runs are seeded and command lines are listed in the three results files.
- Runtime remained lightweight (stdlib-only script, no CAS dependency).

## Minimal script updates added
- `--mode random|stratified|perturbation`
- `--stratified-bounds`, `--samples-per-bound`
- `--perturb-trials`, `--perturb-noise-bound`
- deterministic per-stratum seed derivation from base seed
- explicit proxy-baseline label for perturbation mode

## Next safe extensions (optional)
1. If exact van Luijk polynomial is supplied locally, add a separate `--perturbation-baseline=exact_van_luijk` path and keep proxy mode distinct.
2. Add multi-seed replication table for each stratum to reduce seed variance.
3. Keep theorem/heuristic labels in every new report section.

## Literature tags used here
- [NL-classical]
- [Fermat-classical]
- [vanLuijk-method]

(These are short memo tags only; this pilot does not attempt bibliography reconstruction.)
